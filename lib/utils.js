const Util = {
  dir(vec) {
    const norm = Util.norm(vec);
    return Util.scale(vec, 1/norm);
  },

  dist () {

  },

  norm (vec) {

  },

  randomVec(length) {

  },

  scale (vec, m) {

  }
};

module.exports = Util;




const post = null;

const vel = null;

const color = ['blue', 'green', 'red', 'yellow'];
